{
	"name": "rpg-system-backend-node",
	"version": "0.5.0",
	"description": "Uma API de sistema de inscrições de mesas de RPG em Node usando typescript.",
	"main": "index.js",
	"scripts": {
		"start": "tsx server.ts",
		"start:dev": "tsx watch server.ts",
		"db:dev": "prisma migrate dev",
		"db:seed": "tsx prisma/seed.ts",
		"build": "tsup src",
		"test": "vitest",
		"test:watch": "vitest --watch",
		"test:ui": "vitest --ui",
		"test:run": "vitest run",
		"test:coverage": "vitest run --coverage",
		"test:db:start": "docker compose -f docker-compose.test.yml up -d && npx wait-port localhost:5433 && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npx prisma migrate deploy",
		"test:db:stop": "docker compose -f docker-compose.test.yml down -v",
		"test:docker": "docker compose -f docker-compose.test.yml up -d && npx wait-port localhost:5433 && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npx prisma migrate deploy && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npm run test:run && docker compose -f docker-compose.test.yml down -v",
		"test:docker:watch": "docker compose -f docker-compose.test.yml up -d && npx wait-port localhost:5433 && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npx prisma migrate deploy && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npm run test:watch",
		"test:docker:coverage": "docker compose -f docker-compose.test.yml up -d && npx wait-port localhost:5433 && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npx prisma migrate deploy && cross-env DATABASE_URL=postgresql://test:1234@localhost:5433/test npm run test:coverage && docker compose -f docker-compose.test.yml down -v"
	},
	"prisma": {
		"seed": "tsx prisma/seed.ts"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/bcryptjs": "^2.4.6",
		"@types/cors": "^2.8.18",
		"@types/express": "^4.17.21",
		"@types/jsonwebtoken": "^9.0.9",
		"@types/node": "^22.5.5",
		"@types/supertest": "^6.0.3",
		"@types/swagger-jsdoc": "^6.0.4",
		"@types/swagger-ui-express": "^4.1.8",
		"@vitest/coverage-v8": "^3.2.4",
		"@vitest/ui": "^3.2.4",
		"cross-env": "^10.0.0",
		"globals": "^15.9.0",
		"openapi-types": "^12.1.3",
		"prisma": "^6.7.0",
		"supertest": "^7.1.1",
		"tsup": "^8.3.0",
		"tsx": "^4.19.1",
		"typescript": "^5.6.2",
		"vitest": "^3.1.2",
		"wait-port": "^1.1.0"
	},
	"dependencies": {
		"@prisma/client": "^6.7.0",
		"bcryptjs": "^3.0.2",
		"cors": "^2.8.5",
		"dotenv": "^16.5.0",
		"express": "^4.21.0",
		"jsonwebtoken": "^9.0.2",
		"swagger-jsdoc": "^6.2.8",
		"swagger-ui-express": "^5.0.1",
		"zod": "^3.24.3"
	}
}
